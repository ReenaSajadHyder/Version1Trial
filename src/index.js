const index = (a, b) => a - b;

const mathOperations = {
  sum: function (a, b) {
    return a + b;
  },

  diff: function (a, b) {
    return a - b;
  },
  product: function (a, b) {
    return a * b;
  },
};

// export default subtract;
module.exports = index;
